# اصلاحات اصلی سیستم پلاک‌خوان

به درخواست شما، سبک‌سازی وزن‌ها و ارزیابی سرعت برای مرحلهٔ بعد باقی مانده است. وزن‌های اصلی تغییر نکرده‌اند.

## اصلاحات انجام‌شده

- در detector مقدار precision صریح تنظیم می‌شود: quantize=16 برای GPU و quantize=32 برای CPU در نسخه‌های جدید؛ پارامتر half در نسخه‌های قدیمی حفظ می‌شود. مقدار bool قدیمیِ checkpoint روی CPU هم وارد quantize نمی‌شود. مرجع: https://docs.ultralytics.com/modes/predict/
- متن فارسی پس از reshape و bidi با موتور BASIC در Pillow رسم می‌شود تا دوباره معکوس نشود. گروه پلاک ترتیب فیزیکی عددها و حرف را حفظ می‌کند. کاراکترهای مخصوص نمایش وارد دیتابیس نمی‌شوند.
- پوشهٔ فونت Qt اگر وجود نداشته باشد با پوشهٔ فونت نصب‌شده جایگزین می‌شود.
- pause در live_test همان فریم را چند بار به OCR و رأی‌گیری نمی‌دهد. دوربین از دریافت‌کنندهٔ مستقل استفاده می‌کند؛ ویدئوی فایل به ترتیب خوانده می‌شود.
- رأیِ تأییدشدهٔ پلاک قبلی نمی‌تواند عکس خودروی جدید با خوانش متفاوت را ثبت کند.
- پس از تأیید پلاک، عکس کامل محیط، برش پلاک و نسخهٔ پردازش‌شده در captures ذخیره می‌شوند. جدول captures در دیتابیس تنظیم‌شده (پیش‌فرض data/vehicles.db) شامل timestamp با منطقهٔ زمانی، plate، full_frame، plate_raw، plate_enhanced، det_conf و ocr_conf است.
- عکس‌ها روی دیسک هستند و مسیر مطلقشان در دیتابیس قرار می‌گیرد. جدول vehicles برای اطلاعات راننده/خودرو باقی می‌ماند؛ مشاهدهٔ پلاک جدید به معنای ثبت یا مجازکردن آن خودرو نیست.
- خطای نوشتن عکس گزارش می‌شود و شمارندهٔ failed افزایش می‌یابد. خروج، صف نوشتن را پیش از بستن دیتابیس تخلیه می‌کند.
- live_test در حالت کامل ذخیرهٔ خودکار دارد. OCR-only برای آزمایش کادر وسط است و ذخیرهٔ رویداد کامل YOLO را انجام نمی‌دهد.
- برچسب‌خوانی آموزش و تست دیتاست مشترک شد و شکل‌های نمایشی Unicode نرمال می‌شوند. تست ۹۹٪ ضعیف نشده و نام‌های نامعتبر را گزارش می‌کند.
- آماده‌سازی OCR از بازنویسی تصاویر تکراری جلوگیری می‌کند. اگر برشِ لازم برای برچسب ناقص شکست بخورد، نمونه با تصویر ناهماهنگ وارد آموزش نمی‌شود.
- خروجی جدید OCR پوشه‌های train / val / test دارد. آموزش با ساختار قدیمیِ تخت هم سازگار است. دادهٔ test دیگر به val تشخیص یا دادهٔ آموزشی OCR اضافه نمی‌شود. هم‌پوشانی پلاک train و val در ساختار جدید از آموزش حذف می‌شود.
- آموزش OCR گزارش‌های .history.json و .split.json کنار وزن خروجی می‌نویسد و توقف زودهنگام دارد. بررسی طول CTC تکرارهای مجاور را هم حساب می‌کند.

## تست روی سیستم شما

از ریشهٔ پروژه و در محیط پایتون دارای وابستگی‌ها:

~~~bash
python -m pytest
python -m tools.live_test
~~~

برای وب‌کم یا تغییر منبع:

~~~bash
python -m tools.live_test --source 0
python main.py --source 0
~~~

پیش‌فرض شخصی فعلیِ منبع live_test حفظ شده است. برای دوربین شبکه آدرس خودتان را به --source بدهید.

روی سیستم دارای دیتاست:

~~~bash
python -m tools.audit_dataset --data data/ocr_dataset --output reports/dataset-audit.json
python -m pytest tests/test_plate_utils.py -v
~~~

گزارش کوچک reports/dataset-audit.json و خروجی تست را ارسال کنید. ابزار audit فقط فایل‌ها را می‌خواند.

## موارد نیازمند دادهٔ واقعی

- علت قطعی شکست TestAgainstRealDataset بدون نام فایل‌های مشکل‌دار همان دیتاست مشخص نیست؛ نرمال‌سازی به‌تنهایی تضمین عبور آن تست نیست.
- از وزن نهایی به‌تنهایی نمی‌توان نبود overfitting را اثبات کرد. گزارش‌های جدید برای آموزش‌های بعدی هستند.
- OCR-only محل پلاک را پیدا نمی‌کند؛ پلاک باید کادر را پر کند. سنجش فاصله و زاویه با YOLO+OCR و تصاویر واقعی انجام می‌شود.
- معماری فعلی در هر فریم یک پلاک اصلی را پردازش می‌کند؛ پوشش هم‌زمان چند خودرو به ردیابی مستقل نیاز دارد.
- هشدار HEVC مربوط به دیکود جریان دوربین است؛ اصلاح quantize کیفیت آن جریان را تأیید نمی‌کند.
- پاک‌سازی تصاویر تابع max_files و max_age_days است؛ پس از انقضای نگه‌داری ممکن است عکس حذف شود ولی تاریخچهٔ دیتابیس بماند.
- سبک‌سازی و benchmark طبق درخواست آخر شما به مرحلهٔ بعد موکول شد.

## وضعیت بررسی محلی

- صحت نحوی ۴۰ فایل پایتون بررسی شد.
- بررسی مستقل نرمال‌سازی فارسی، خواندن پسوند تکراری، ثبت پایدار SQLite و گزارش دیتاست موفق بود.
- اجرای تست‌های مرتبط با اصلاحات اصلی بدون شکست تمام شد؛ موارد وابسته به دیتاست واقعی و فونت محیط skip شدند.
- اجرای سراسری بعدی در محیط WSL متوقف ماند و قطع شد؛ موفقیت کل مجموعه‌تست یا دقت وزن‌ها ادعا نمی‌شود.
- تست دوربین و دیتاست واقعی بر عهدهٔ اجرای بعدی شماست.
